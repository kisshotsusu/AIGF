# 当前状态与开发进度

## 2026-07-21 提示词唤醒直接输入

- HomeAgent 新增 `prompt_wake` 配置节，支持通过唤醒词直接输入命令。
- 启用后，语音输入以唤醒词开头时会自动提取后面的命令并执行，例如"苏苏，打开浏览器"会自动识别为"打开浏览器"。
- 设置窗口新增"唤醒"标签页，可配置唤醒词列表、启用/禁用唤醒功能、自动发送选项等。
- 默认唤醒词：`["苏苏", "小助手", "你好苏苏", "嘿苏苏"]`，可通过设置界面自定义。
- 唤醒检测在 `agent.py` 的 `chat` 方法中实现，位于重启请求检查之后、常规对话处理之前。


## 2026-07-21 绝对路径代码检查修复

- 最新日志中 `code_list_files(E:\\Doc\\AIAgent\\modules\\live)` 被“只接受相对路径”拒绝，而通用文件工具随后能读取同一路径；现已统一只读权限，完整磁盘访问开启时 `code_list_files/read_file/search_text` 支持绝对路径。
- 完整磁盘访问现同时授权外部绝对路径写入和替换，外部修改会进入变更跟踪与语法校验；`.env`、PEM、KEY、PFX 等密钥文件始终拒绝。
- 修复“检查开发文档”因包含“开发”二字被误判为“开发新项目”的分类问题；纯检查任务不再被强制要求产生代码变更。

## 2026-07-21 重启任务恢复修复

- 修复 `task-recovery.json` 在任务完成后仍永久保留的问题：普通任务完成或取消后现在立即删除。
- 修复自升级成功重启时把已完成原任务再次自动提交的问题：`restart_pending` 明确写入 `task_completed: true`，新进程只清理状态并加载新代码，不再执行原提示词。
- 只有状态为 `running` 的真实中断任务会生成恢复提示并自动继续；旧的 `completed`、`cancelled` 和 `restart_pending` 状态会在读取时清理。
- 二次修复直接重启的短路漏洞：此前 `direct_restart or self_upgrade.finalize()` 在左侧为真时跳过 `finalize`，遗留 `running: 重启你自己`；现直接重启不再创建恢复文件，收尾显式清理，读取旧遗留记录时也会删除且不恢复。

## 2026-07-21 HomeAgent 输入队列

- Qt 默认界面在当前任务执行及最终结果语音播放期间不再丢弃新输入；文字发送和语音识别自动发送都会进入同一 FIFO。
- 当前 `ChatWorker` 完整结束后自动执行下一项，保持任务与 TTS 串行；界面显示排队数量，发送输入始终可用。
- 停止按钮只停止当前任务并保留后续队列；重启请求不会在旧进程中启动排队任务。
- `HomeAgent/tests/test_input_queue.py` 覆盖截图缩略图预览与清理、忙碌入队、完成后出队和重启门禁。

## 2026-07-21 最新自升级失败修复

- 22:14 的“直播间欢迎观众功能失效，检查并修复”因缺少直播组件关键词而被误判为普通对话，最终在 UI/命令工具中耗尽 28 轮；现已将直播、直播间、欢迎观众、弹幕、B站/Bilibili、角色管理器、Vision、Sound 等工程组件纳入自身代码任务识别。
- 工具循环耗尽或 Codex 后备结束后的 TTS 原先复用已经关闭的 `aiohttp` 会话，日志出现 `Session is closed`；现改为通过 `_speak_with_fresh_session` 创建独立有效会话后播报失败结果。
- 新增回归测试覆盖原始失败指令和工具循环结束后的有效 TTS 会话。
- 原 `max_tool_rounds: 28` 会让成功的调查步骤也占用轮次；现改为最多累计 28 个失败轮次，成功工具调用不扣额度，并以 `max_tool_iterations: 112` 作为独立防失控上限。
- 失败结果通过新会话进入现有 `TTSClient`/GPT-SoVITS 主链路；仅当该主链路抛出真实异常时才降级 Windows SAPI。测试明确保证 GPT-SoVITS 成功时不会调用系统语音。

## 2026-07-21 本地直接重启

- “重启自己”“请现在重启你自己”“重启 HomeAgent/桌宠”等直接命令不再发送给大模型，HomeAgent 会在模型规划前本地识别并执行。
- Qt 主界面和 Tk 兼容界面都会先显示“正在重启”，再启动看门狗、退出旧进程并拉起新实例。
- “不要重启”“如何重启”“能否重启”等否定或咨询表达，以及“增加重启功能”等开发需求不会误触发直接重启。

## 2026-07-21 自主升级可靠性完善

- 已修复“升级自己的代码”未被识别为自升级的问题，并覆盖“自己、自己的代码、自动升级、自我升级”等表达。
- 自升级结束时强制要求存在真实代码或配置变更、同步 `AI Read` 并通过本地校验；`changed_files: []` 不再允许显示成功或触发完成状态。
- MiMo 把 `<tool_call>`、`<function=>`、`<parameter=>` 写入普通文本时，HomeAgent 会拒绝该回答并要求结构化工具调用，不再将伪调用显示为完成结果。
- 含伪工具标签或代码块的回答不会进入 TTS，避免把未执行代码拆成大量语音朗读。

## 2026-07-21 HomeAgent 主动屏幕关怀

- HomeAgent 运行时默认每 300 秒截取一次主屏幕，使用现有 MiMo `mimo-v2.5` 图片理解能力生成一句问候、鼓励或休息提醒。
- 用户任务执行中以及前一轮关怀尚未结束时自动跳过，避免抢占、重叠和定时任务堆积。
- 提示词禁止复述账号、聊天、文件内容、密码、验证码、金额等屏幕隐私；截图使用系统临时文件并在成功或失败后立即删除。
- 关怀语默认显示并播报；设置窗口新增“屏幕关怀”页，可直接启用/关闭并设置 1～1440 分钟的问候频率，保存后立即更新运行中的定时器。高级显示和播报选项仍可通过 `HomeAgent/config.yaml` 的 `screen_care` 调整。
- 每次问候还会在桌宠旁显示独立消息气泡；气泡不抢键盘焦点、自动避让屏幕边缘，并默认在 12 秒后消失。

核对日期：2026-07-21。本文件记录“现在是什么”，历史实现不应覆盖此处事实。

## 当前配置快照

| 项目 | 当前值 |
|---|---|
| B站房间 | `21248701` |
| 直播触发 | `all` |
| 真实弹幕 | `send_danmaku: false`；关闭 |
| 根 LLM | MiMo `mimo-v2.5` |
| Token | Live 300 / Home 800 |
| TTS | 9879，自动启动，60 秒请求超时，4 次尝试，2 秒起始退避 |
| STT | `sound_mcp`，8766 |
| MiMo 多模态 | 图片 `mimo-v2.5`；ASR `mimo-v2.5-asr`；任务完成检查启用、失败重试 2 次、fail-closed |
| Vision MCP | 8765，`gui_enabled: true`，`preload_model: false` |
| HomeAgent 执行 | 模型语义规划、本地工具优先、默认 4 轮操作重试 |
| Codex | 已启用，隔离 Home，600 秒普通任务超时，仅作后备 |
| 上下文维护 | 直播 120 分钟；家庭每天 03:00；`work/` 保留 3 天 |

## 已完成的主要阶段

### 1. 项目和配置整理

- 直播代码归入 `modules/live/`，根入口保留兼容转发。
- 主要模块目录使用英文；项目根目录现为 `AIAgent`，运行代码必须基于自身文件位置解析根目录，不得依赖固定盘符路径。
- 大部分项目内路径改为相对根目录解析；外部 GPT-SoVITS 启动路径仍由配置明确给出。
- 工具维护拆分到 `HomeAgent/config.d/`，角色管理服务原子保存并保留未知配置。

### 2. Qt 界面迁移

- HomeAgent 默认使用 PySide6 无边框圆角窗口，保留桌宠、收起/展开、文字、语音、停止和设置功能。
- 发送与停止按钮并列；任务执行时展示当前步骤、完成内容和耗时。
- 角色管理器默认使用 PySide6，数据访问抽象到 `CharacterService`；Qt 已补身份、外观、记忆规则、模型 API、麦克风设备、Vision/MCP、工具维护和路径选择等配置页。
- MiMo 图片、语音和完成检查配置已合并到“模型 API → MiMo 多模态”标签，不再使用独立侧栏页面；配置按认证、能力模型、完成检查分成三张高对比卡片，主体支持纵向/横向滚动，保存按钮固定在底部，密钥只显示配置状态。
- Tk 前端仍保留 `--legacy-tk` 后备，不再作为默认界面。

### 3. 语音链路

- HomeAgent 连接 Sound MCP/SenseVoice，语音识别结果可自动发送。
- HomeAgent 也可按配置使用 MiMo `mimo-v2.5-asr` 识别 WAV/MP3；音频 Base64 后限制 10 MB。
- 处理中文路径、编码乱码和不支持字符；TTS 输出自动清理为最新 20 个。
- 直播增加独立优先语音队列：欢迎语优先、合成与播放串行。
- 修复欢迎冷却提前写入、双进场事件重复、显卡繁忙单次超时即丢任务，以及健康检查超时重复启动语音服务的问题。
- 直播 TTS 暂态错误默认重试 4 次；欢迎、语音排队、成功与最终失败写入 `messages.jsonl`。

### 4. 网页与电脑自动化

- `web-agent-operator` 从“打开网页”升级为完整状态机，搜索、选择、播放、提交必须验证最终结果。
- 优先检查现有浏览器和 CDP DOM；登录任务不强制创建新 Chrome。
- B站收藏夹读取真实数据和真实顺序，导航已打开浏览器并验证最终 BVID。
- 网易云搜索播放要求精确选择歌曲结果，避免点击底部全局播放导致继续当前歌曲。
- 点击、双击、输入、快捷键和滚动后约 550 ms 自动截图，返回状态变化与下一步。
- GUI-Actor 当前允许懒加载；服务启动不预加载，降低常驻显存占用。

### 5. Agent 任务能力

- MiMo 先生成语义任务合同；本地代码验证并执行，不再把目标理解主要建立在站点正则上。
- HomeAgent 本地工具优先，Codex 只处理复杂编程、终端、自升级或本地能力缺失场景。
- 自编程请求会在委派前实际读取 README 和核心 `AI Read` 文档，并强制检查工作区、写入文件、运行校验；零变更任务按失败处理。
- 自编程实现已从主程序抽离到 `HomeAgent/home_modules/code_editor.py`；`agent.py` 只负责路由，`self_upgrade.py` 只负责状态和重启恢复。
- Agent 可创建持久化独立项目，默认位于 `Projects/<project-name>/`；代码写入后由本地模块独立执行编译、Python 测试、Node 语法/npm 测试、TypeScript 检查或静态页面结构检查。
- 代码任务不再自动转交 Codex：MiMo 优先调用 HomeAgent 内置的列目录、读取、搜索、原子写入、精确替换和测试工具；Codex 只在明确点名或本地工具耗尽后后备调用。
- 本地代码校验或测试失败时会把真实输出反馈给编码执行器，自主修复并重新测试，默认最多 2 轮，耗尽后才报告失败。
- 最终回答通过 UI 回调先于 TTS 播放显示，Qt 默认界面和 Tk 后备界面均不会再等待整段语音结束。
- Codex 工具链修复强制 MCP 验证、超时边界、stdin/JSONL、CLI 模型缓存版本兼容和实时进度解析。
- 长任务提供 UI 实时进度和可选 TTS 汇报；失败保留阶段、尝试次数和原因。
- 自主升级支持重启前验证、任务状态持久化、重启后继续和最大重启次数限制。
- 执行类任务新增 MiMo 独立完成检查：读取工具证据而非助手口头声明，检查失败会继续修正，默认最多回灌 2 次。
- 自编程校验已强制实现与 `AI Read` 同步；代码或配置改变但说明未更新时验证失败。

### 6. 记忆与维护

- 直播、家庭共享身份和高价值记忆，私密内容与直播提示词隔离。
- 直播上下文可在助手未运行时清空，并通过控制文件同步运行时 deque。
- 重要度滑条、每日上限、API 已配置提示、设备名称和工具文档等配置已进入角色管理 UI。
- HomeAgent 定期清理 `work/`，默认保留 3 天。

## 本轮新增验证

HomeAgent 当前 78 项测试通过，其中覆盖 MiMo 图片请求结构、剪贴板截图与缩略图预览、ASR 请求结构、格式限制、完成检查 JSON、证据时间排序、媒体停止与强制终止区分、Codex stdin、CMD 引号、GB18030 日志、截图重试、非法工具参数、截断响应、模型规划路由与主动关怀上下文；Vision 7 项窗口/截图/媒体停止可靠性测试和直播 5 项测试通过。

`modules/live/tests/test_reliable_speech.py` 已覆盖：

- TTS 前两次模拟 GPU busy、第三次恢复后成功。
- TTS 失败不写欢迎冷却，再次进场仍能尝试。
- 同一用户双进场事件只产生一个待播放欢迎。

运行：

```powershell
.venv\Scripts\python.exe -m unittest discover -s modules/live/tests -v
```

## 已知限制与风险

- 普通 Chrome/Edge 未以调试端口启动时无法读取其 DOM，只能保留该窗口并使用视觉操作。
- B站登录、风控、广告和页面实验可能改变 DOM；收藏夹 API 或登录 Cookie 失效时必须报告真实失败。
- GUI-Actor 在双 4K 屏幕和复杂界面上仍可能误定位；操作后截图只能证明状态变化，不能单独证明业务目标正确，仍需读取文本/URL/媒体状态。
- GPT-SoVITS 长时间显存不足时，4 次重试后仍会最终失败，但不会静默丢失：日志会记录具体错误，欢迎冷却不会被消耗。
- 进场欢迎依赖 WebSocket；当前兼容四种新旧事件名、使用登录 Cookie 身份鉴权、记录连接异常，并以文件锁拒绝重复直播进程。历史弹幕接口仍只能补弹幕，不能补回断线期间的进场事件。
- Codex 网络和远端 MCP 可能返回 5xx 或不完整 JSONL，普通网页任务不得依赖它作为首选。
- 项目中仍保留 Tk 兼容代码与部分历史说明，修改时要确认真实启动入口。
- `[2026-07-20 文档细化]` `HomeAgent/config.yaml` 的 `computer_control.applications` 有 6 项（含 `网易云音乐`/`cloudmusic`），而 `HomeAgent/config.d/computer_control.yaml` 只有 4 项；两者未对齐，`CharacterService` 下次读取会按 mtime 取其一。建议统一基准后再改。
- `[2026-07-20 文档细化]` 新增 `07_DEVELOPER_REFERENCE.md`：函数级 API、数据契约、配置同步机制与已知坑；`03` 补全完整 Vision MCP 工具集并新增 `semantic_planner`/`progress_reporting` 两节；`02` 标注 `src/ai_live_assistant` 为 shim、`task_manager` 实际位置。

## 下一步优先级

1. 在真实直播间做低风险进场测试，统计 `welcome: received/success/error/skipped` 比例与语音队列等待时间。
2. 为语音队列增加可视化长度、当前重试轮次和最近错误，便于区分事件缺失与 GPU 阻塞。
3. 增加 B站 WebSocket 连接/断线日志与重连计数，评估无法由历史接口补回的进场事件比例。
4. 对网易云、B站收藏夹和通用网页状态机增加无声音/无外部写入的自动回归测试。
5. 做一次双 4K、GUI-Actor 懒加载与 GPT-SoVITS 并存时的显存基线测试。

## 排障入口

1. 直播欢迎：先查 `logs/messages.jsonl` 的 `welcome` 和 `speech`，再查 `logs/assistant.log` 的重试记录。
2. HomeAgent 路由：查 `HomeAgent/logs/agent-events.jsonl` 的任务计划、路由、步骤和失败阶段。
3. 网页/视觉：查 `Vision/logs/vision-mcp.log`，确认使用 existing CDP、独立 Playwright 还是 window visual。
4. STT：查 `Sound/logs/sound-mcp.log`。
5. 重启恢复：查 `HomeAgent/state/task-recovery.json`。
## 2026-07-21 全套常驻服务单实例修复

- 总控制台、HomeAgent、角色管理器、直播助手、Sound MCP、Vision MCP 已统一采用跨进程非阻塞文件锁，消除并发启动时的端口探测竞态和重复模型加载。
- HomeAgent 不再主动删除 30 秒以上的 Qt 锁文件，避免长时间运行的真实实例被误判为陈旧锁。
- Windows 虚拟环境启动器与基础 Python 子进程仍可能显示为两个 PID，但属于同一逻辑实例。
## 2026-07-21 关怀消息代码路由误触发修复

- “写代码累了、喝口水、休息一下、回复模糊语义号”等关怀文本或界面辅助标签不再被当作代码指令。
- 同一句包含“修复、修改、实现、重构”等明确动作时仍按代码任务处理，避免简单屏蔽“写代码”造成漏判。
## 2026-07-21 提醒消息与语音同步修复

- 定时提醒在 TTS 合成和播放前立即通过 Qt 信号显示到聊天区及桌宠气泡；语音继续在后台播放。
- TTS 缓慢、显卡繁忙或最终失败不再延迟或吞掉可视提醒。
## 2026-07-21 主动关怀回复上下文修复

- 日志确认主动关怀过去只显示、不进入 `self.history`，导致用户回复“好的”时模型沿用旧代码任务；现已在可视提示和 TTS 前写入助手上下文。
- 关怀消息现先同时显示聊天消息和桌宠气泡，再后台播放语音，不再等待 TTS 完成。
## 2026-07-22 屏幕读取改为模型任务分析

- 删除“看看我在做什么”等硬编码短语分流和固定“描述用户活动”提示；语义规划器现在会分析所有请求并输出视觉需求与交互模式。
- 新增模型可调用的 `ui_analyze_screen`，支持观察当前活动、读取屏幕题目并求解、识别游戏局面；游戏操作要求一步一观察，禁止盲目连续点击或按键。
- 视觉路由由 `task_plan.visual_required` 决定，普通文本即使包含“屏幕”字样也不会绕过模型计划直接截图。

## 2026-07-22 总任务规划改造

- MiMo 现在先判定消息是否为任务，再决定直接回答、执行或追问，并给出执行策略、所需能力、首选工具、步骤、风险和成功条件。
- 删除本地关键词对模型 `actionable` 结果的强制合并、B站简单打开捷径以及网页关键词二次分流；站点专用处理器只根据模型输出的结构化站点与操作字段选择。
- 规划上下文同时包含助手主动关怀及其 `source` 和用户回复，避免“好的”等确认语被误识别为旧代码任务。

## 2026-07-22 MiMo 工具调用可靠性修复

- 保持现有提示词 JSON 方案，不启用结构化输出 `response_format`。
- 主工具循环现在检查 `finish_reason`；`length`、`content_filter`、`repetition_truncation` 的响应不会显示、解析或执行其中工具。
- 工具 `arguments` 必须解析成 JSON 对象；格式错误时回传该 `tool_call_id` 的失败结果并要求模型重试，禁止退化为空参数执行。
- 完成检查的 `passed` 以及家庭/直播记忆分类的 `should_remember` 必须是真正的 JSON boolean，避免字符串 `"false"` 被 Python 当成真值。

## 2026-07-22 剪贴板截图输入

- 默认 Qt 和 Tk 后备输入框均支持直接粘贴系统截图；Qt 显示尺寸与移除入口，截图可以在无文字时直接发送。
- 图片作为本轮 MiMo 多模态用户消息直接输入，不需要先写入项目目录或再调用图片分析工具；历史只保留附件标记。
- 忙碌时截图和文字作为同一个 FIFO 项排队；每张临时 PNG 在任务完成、取消、替换或退出时清理。

## 2026-07-23 对话框截图预览修复

- 原实现只显示“已粘贴截图 · 宽×高”文字，未创建图片控件，因此附件存在但无法预览内容。Qt 输入区现显示 180×96 等比例缩略图，并保留尺寸标签和移除按钮。
- 无效或空 QImage 会明确报错；替换、移除、发送和退出继续清理预览及临时 PNG，不影响送入 MiMo 的原始分辨率图片。

## 2026-07-23 Codex 宠物技能项目化

- Codex 的 `hatch-pet` 技能已完整复制到 `Skill/hatch-pet/`，保留 `SKILL.md`、脚本、参考规范、代理配置、许可证和测试，不包含 Python 缓存产物。
- 项目副本明确从当前 `Skill/hatch-pet` 解析脚本目录，避免运行时重新指向用户目录下另一份 Codex 技能。Home Agent 的本地技能扫描会自动发现 `hatch-pet`。

## 2026-07-22 最近任务日志错误修复

- 16:42 的编译进度任务已成功读到 Visual Studio `396/920`（约 43%），但完成检查把只读观察误当成必须出现业务终态，随后继续调用窗口、命令和文件工具；核验器现按任务类型判断，只读观察取得所问信息即完成，真实 MiMo 请求已返回 `passed=true`。
- MiMo 完成检查显式关闭 thinking、关闭流式响应并使用足够的输出预算；空内容会附带 `finish_reason` 明确报错。仍维持提示词 JSON 与本地类型校验，不使用 `response_format`。
- HomeAgent 与 Vision 截图统一串行、最多重试 3 次；窗口截图优先使用 HWND，再退回边界截图，整屏 GDI 不可用时退回当前前台窗口。操作后截图失败改为未验证失败，不再伪造 `state_changed=true`。
- `run_cmd` 改用 Windows CMD 的原生字符串调用，修复内部双引号被转义后 `tasklist /fi "imagename eq devenv.exe"` 报 `Invalid argument - eq`。
- `read_text_file` 在 UTF-8 之外支持带 BOM 的 UTF-16 与 GB18030 文本日志，同时继续拒绝含 NUL 的二进制内容；最近失败的 `logs/assistant.log` 与 `Vision/logs/vision-mcp.log` 均已实读验证。

## 2026-07-22 音乐停止确认与子升级修复

- 最近日志中的音乐任务先发送 Space 暂停、随后再次发送 Space 恢复播放，又误用点击、Alt+F4 和 `Stop-Process`；最终核验还用旧窗口证据覆盖较新的进程状态。现在停止音乐只走幂等系统 Media Stop，明确退出/终止进程的任务仍保留强制终止能力。
- 工具、Vision 请求、窗口截图与分析结果统一记录带时区的提交/采集/完成时间和递增序号；MiMo 完成检查按最新证据判断，分析完成较晚不会让旧截图变成当前状态。纯黑 HWND 截图自动退回窗口边界截图。
- 子升级日志中的 112 次无进展读取来自搜索命中后反复读取 `agent.py` 文件开头。代码读取新增 `start_line/max_lines`，连续 8 次只读会纠偏，12 次仍无编辑则熔断并交给后备执行器。
- Codex 后备任务不再把大型开发合同作为 Windows 命令行参数，而使用 `codex exec ... -` 从 stdin 接收；进程启动/提示投递异常会写明失败并把恢复状态标记为 `failed`，不会在重启后重复执行。真实只读冒烟已确认 CLI 能启动、接收 stdin 并返回 `turn.completed`；本次网络曾发生 WebSocket 超时并自动退回 HTTPS。
- 重启后的真实 Vision MCP `desktop_media_stop` 已返回 `ok=true`、`requested_state=stopped`、`idempotent=true` 与 `action_sent_at`；8765 只有一个监听进程，失败升级遗留的 `task-recovery.json` 未被重新创建。
