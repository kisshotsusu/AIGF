# Vision 双后端依赖 (GUI-Actor / GUI-Owl)，统一运行于项目共享 .venv
# Python 3.12 推荐
torch
torchvision
# GUI-Actor (Qwen2-VL) 与 GUI-Owl (Qwen3-VL) 现共用 transformers>=4.57 + qwen-vl-utils>=0.0.14
# （已验证 GUI-Actor 在 transformers 4.57 下仍正常加载，故无需分离 venv）
transformers==4.57.1
qwen-vl-utils==0.0.14
accelerate
pillow
numpy
opencv-python-headless
playwright
mcp[cli]
huggingface_hub
